import Keyboard from './keyboard.js';
import './updateTheme.js';

/**
 * Future version will have:
 * 1. a number of additional instruments
 * 2. ability to switch between them
 * 3. other optimizations
 */

const instrument = 'piano';
new Keyboard(instrument);
